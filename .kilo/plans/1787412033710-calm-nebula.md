# Plan: Remove public property LIST, keep public CREATE + update docs

## Goal
In **Nexa-Client-BE**, remove the unauthenticated (public) **list** endpoint for properties while keeping the public **create** endpoint. Then update **Nexa-Documentation** so the documented public surface is create-only.

---

## Part A — Code change (Nexa-Client-BE)
File: `app/interface/routes/property.py`

### Current state (verified by reading the file)
The `public_property_router` (mounted at prefix `/public` in `app/interface/routes/__init__.py:28`) currently has exactly two routes:
- `list_public_properties` — `GET /public/sites/{site_id}/properties` (lines **289–346**) ← **REMOVE**
- `create_public_property` — `POST /public/sites/{site_id}/properties` (lines **349–392**) ← **KEEP**

The authenticated `property_router` (no `/public` prefix) keeps all of its routes unchanged:
`create_property` (POST), `list_properties` (GET, Bearer), `get_property`, `update_property`, `delete_property`.

### Edit
1. Delete the `list_public_properties` function and its `@public_property_router.get(...)` decorator (lines 289–346), including the trailing blank line before the POST decorator.
2. Keep the `# Public routes (prefix /public)` header comment (line 284–286) since it still introduces the kept `create_public_property`.
3. Keep `create_public_property` exactly as-is.

### Import / type safety check (no change expected)
- `Request` — still used by `create_public_property` (line 355).
- `Settings` — still used by `create_public_property` (line 359).
- `Query`, `Optional` — still used by the authenticated `list_properties` (lines 115–122) and elsewhere.
- `PropertyListResponse` — still used by the authenticated `list_properties` (lines 111, 153).
- No other code references `list_public_properties` (grep confirmed: no other file imports/uses it).

### Tests
- No integration/unit tests reference the public list or `list_public_properties` (grep of `tests/` for `public|/public|list_public|create_public` returned only unrelated id-helper tests).
- The existing `test_list_properties_without_auth_returns_401` (authenticated list → 401) stays valid.
- No test files need editing.

---

## Part B — Documentation updates (Nexa-Documentation)

Finding: the public **list** endpoint (`GET /public/sites/{site_id}/properties`) is **not documented anywhere** — so there is nothing to delete from the docs. The docs already show `POST /public/...` (No auth) for create and `GET /sites/...` (Bearer) for list. The doc work is to (1) make the create-only public surface explicit, and (2) fix two places that incorrectly name the public create endpoint without the `/public` prefix.

### B1. `specs/core/api.md` — clarify public surface (Property table ~line 138–145)
After the Property endpoint table (around line 145), add a note, e.g.:
> **Public (unauthenticated) endpoints:** only `POST /public/sites/{site_id}/properties` (create) is public. There is **no** public list endpoint — listing requires `Bearer` auth via `GET /sites/{site_id}/properties`.

(No row needs removal; the table already has no `GET /public/...` line.)

### B2. `tenants/nexa_tenant/sites/muot-spa/booking.md` (line 23)
Change:
> **Public access:** `POST /sites/{site_id}/properties` accepts unauthenticated requests for `booking_submission` creation.
To:
> **Public access:** `POST /public/sites/{site_id}/properties` accepts unauthenticated requests for `booking_submission` creation. Unauthenticated requests use admin token internally for PocketBase writes; `created_by` is empty for guest submissions. (Listing submissions requires `Bearer` auth via `GET /sites/{site_id}/properties`.)

### B3. `tenants/README.md` (line 23)
Change:
> - Frontend calls `POST /sites/{site_id}/properties` to create bookings
To:
> - Frontend calls `POST /public/sites/{site_id}/properties` to create bookings (unauthenticated)

### Out of scope (already correct — no change)
- `specs/property-model/spa-service.md` (lines 198, 200, 227) already correctly references `POST /public/sites/{site_id}/properties` for create and authed `GET /sites/...` for admin list.
- `specs/property-model/_index.md` API block lists only authenticated endpoints (no public list to remove).

---

## Part C — Verification & deployment

1. **Nexa-Client-BE unit/integration tests** (read-only check):
   `cd /home/phantomal/Nexa-Client-BE && ./venv/bin/python -m pytest -q` (or the repo's test command) — expect green; the authed-list-401 tests still pass, public-list removal introduces no failures.
2. **Live curl checks** (after redeploy):
   - `GET /public/sites/muot-spa/properties` → expect **404** (removed).
   - `POST /public/sites/muot-spa/properties` (valid body) → expect **201**; (empty body) → expect **422** (still present).
   - `GET /sites/muot-spa/properties` (no token) → expect **401** (unchanged).
3. **Redeploy Nexa-Client-BE** via the deploy workflow (no `--migrate`; no `requirements.txt` change). This restarts `nexa-client-be.service` and re-runs the health check.
4. Nexa-Documentation is a docs-only repo — no service to restart; changes are committed separately.

---

## Risks / notes
- **Frontend impact:** Checked `Nexa-FE` — every property-list call (`listPropertiesSitesSiteIdPropertiesGet`) targets the **authenticated** `GET /sites/{site_id}/properties`, and no source references `/public/sites`. So removing the public list will **not** break the live site/Portal. (Out of scope to change frontend.)
- The public create endpoint keeps using server-side admin auth with empty `created_by`, so guest booking submissions continue to work.
- `api-client` generated code in `Nexa-FE` (if it ever generated a public-list function from the Client-BE spec) would simply lose that function on next `pnpm generate:api`; not needed now since it's unused.
